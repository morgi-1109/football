import { ToastDirective } from './toast.directive';

describe('ToastDirective', () => {
  it('should create an instance', () => {
    const directive = new ToastDirective();
    expect(directive).toBeTruthy();
  });
});
